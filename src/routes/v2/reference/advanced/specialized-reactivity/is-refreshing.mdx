---
title: "isRefreshing"
category: "Advanced / Specialized Reactivity & Tracking"
version: "2.0"
description: "Returns `true` while a `refresh()` call is in progress. Useful for showing\na \"refreshing\" indicator distinct from the initial-load `<Loading>`\nfallback."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/core/core.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Returns `true` while a `refresh()` call is in progress. Useful for showing
a "refreshing" indicator distinct from the initial-load `<Loading>`
fallback.

## Import

```ts
import { isRefreshing } from "solid-js";
```

## Type signature

```ts
function isRefreshing(): boolean;
```

## Examples

```tsx
<Show when={isRefreshing()}>
	<span class="badge">refreshing…</span>
</Show>
```

---
title: modifyMutable
use_cases: >-
  batch updates, mutable store modifications, performance optimization, multiple
  field changes, reconciliation
tags:
  - store
  - mutable
  - batch
  - performance
  - updates
  - removed
version: '2.0'
description: >-
  modifyMutable has been removed from solid-js/store in Solid 2.0 alongside
  createMutable. Both have moved to @solidjs/signals.
---

:::caution
In Solid 2.0, `modifyMutable` has been **removed from `solid-js/store`** alongside [`createMutable`](/reference/store-utilities/create-mutable). Both APIs have been moved to the **`@solidjs/signals`** package.
:::

## Migration

Update your imports from:

```tsx
// Solid 1.x (no longer works in 2.0)
import { modifyMutable, reconcile, produce } from "solid-js/store";
```

To:

```tsx
// Solid 2.0
import { modifyMutable } from "@solidjs/signals";
import { reconcile, produce } from "solid-js/store";
```

## Previous Usage

`modifyMutable` streamlined the process of making multiple changes to a mutable Store, as obtained through the use of [`createMutable`](/reference/store-utilities/create-mutable).

It operated within a single [`batch`](/reference/reactive-utilities/batch), ensuring that dependent computations are updated just once, rather than triggering updates for each individual change.

```tsx
import { modifyMutable } from "@solidjs/signals";

function modifyMutable<T>(mutable: T, modifier: (state: T) => T): void
```

## Recommended Alternative

In Solid 2.0, the recommended approach for most use cases is [`createStore`](/reference/store-utilities/create-store) with its draft-first setter, which batches updates automatically:

```tsx
import { createStore } from "solid-js/store";

const [state, setState] = createStore({
	user: {
		firstName: "John",
		lastName: "Smith",
	},
});

// Draft-first setter batches all mutations automatically
setState((draft) => {
	draft.user.firstName = "Jane";
	draft.user.lastName = "Doe";
});
```

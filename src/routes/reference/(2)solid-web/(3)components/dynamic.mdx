---
title: "dynamic"
category: "Components"
use_cases: "components api, dynamic usage"
tags:
  - "dynamic"
  - "components"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Creates a stable component whose rendered component comes from a reactive source. The source can return a component, an intrinsic element name, a promise, or no component."
source_repo: "solidjs/solid"
source_ref: "next"
source_path: "packages/web/src/index.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Creates a stable component whose rendered component comes from a reactive source. The source can return a component, an intrinsic element name, a promise, or no component.

> `dynamic()` is the canonical API. It returns a stable component identity and composes with `lazy()` and routing.

## Import

```ts
import { dynamic } from "@solidjs/web";
```

## Type signature

```ts
function dynamic<T extends ValidComponent>(
	source: () => T | Promise<T> | null | undefined | false
): Component<ComponentProps<T>>;
```

## Examples

```tsx
const CurrentPage = dynamic(() => pages[route()]);

return <CurrentPage />;
```

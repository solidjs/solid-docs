---
title: "Match / Switch"
category: "Components (JSX)"
use_cases: "components (jsx) api, match usage"
tags:
  - "match"
  - "switch"
  - "components"
  - "jsx"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "A branch inside a `<Switch>`. The first `<Match>` whose `when` is truthy\nwins; remaining matches are skipped."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid/src/client/flow.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:match:summary */}
A branch inside a `<Switch>`. The first `<Match>` whose `when` is truthy
wins; remaining matches are skipped.

Like `<Show>`, `<Match>` supports a function child that receives an
accessor for the narrowed value.
{/* /@gen:match:summary */}

{/* @gen:match:import */}
## Import

```ts
import { Match } from "solid-js";
```
{/* /@gen:match:import */}

{/* @gen:match:signature */}
## Type signature

```ts
function Match<T, F extends ConditionalRenderCallback<T>>(props: MatchProps<T, F>);
```
{/* /@gen:match:signature */}

{/* @gen:match:examples */}
## Examples

```tsx
<Switch fallback={<NotFound />}>
  <Match when={user()}>
    {u => <Profile name={u().name} />}
  </Match>
  <Match when={loading()}>
    <Spinner />
  </Match>
</Switch>
```
{/* /@gen:match:examples */}

---

{/* @gen:switch:summary */}
Switches between content based on mutually exclusive conditions. Renders
the first `<Match>` whose `when` is truthy; falls back to `fallback` when
none match.
{/* /@gen:switch:summary */}

{/* @gen:switch:import */}
## Import

```ts
import { Switch } from "solid-js";
```
{/* /@gen:switch:import */}

{/* @gen:switch:signature */}
## Type signature

```ts
function Switch(props: { fallback?: JSX.Element; children: JSX.Element }): JSX.Element;
```
{/* /@gen:switch:signature */}

{/* @gen:switch:examples */}
## Examples

```tsx
<Switch fallback={<FourOhFour />}>
  <Match when={state.route === 'home'}>
    <Home />
  </Match>
  <Match when={state.route === 'settings'}>
    <Settings />
  </Match>
</Switch>
```
{/* /@gen:switch:examples */}

## Related types

### `MatchProps`

```ts
type MatchProps<T, F extends ConditionalRenderCallback<T> = ConditionalRenderCallback<T>> = {
  when: T | undefined | null | false;
  keyed?: boolean;
  children: ConditionalRenderChildren<T, F>;
};
```

---
title: useTransition
use_cases: >-
  async batching, loading states, concurrent updates
tags:
  - transitions
  - async
  - loading
  - removed
version: '2.0'
description: >-
  useTransition has been removed in Solid 2.0. Transitions are now built into the
  runtime. Use Loading and isPending for transition UI.
---

:::caution[Solid 2.0 — `useTransition` is removed]
`useTransition` has been removed in Solid 2.0. Transitions are now a built-in scheduling concept managed by the runtime. Multiple transitions can be in flight simultaneously.
:::

## Replacement

```ts
// 1.x
import { useTransition } from "solid-js"
const [isPending, start] = useTransition()
start(() => setSignal(newValue))

// 2.0 — just update the signal
setSignal(newValue)

// For pending state:
import { isPending } from "solid-js"
const pending = () => isPending(() => asyncMemo())
```

- Use `<Loading>` boundaries for initial loading UI
- Use `isPending()` for stale-while-revalidating indicators
- Use [`action()`](/guides/fetching-data#mutations-with-action) for mutations with optimistic UI

See the [fetching data guide](/guides/fetching-data) for complete patterns.

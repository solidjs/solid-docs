---
title: Attribute name
description: One-sentence factual description of what this JSX attribute or syntax form does.
---

# `attributeName`

Add one or two short sentences that define the syntax and where it applies.

## Syntax

```tsx
<div attributeName={value} />
```

## Value

- **Type:** `ValueType`

Describe the accepted value shape and any important variants.

## Behavior

Describe how Solid interprets, compiles, or applies this syntax.
Keep this section factual and limited to semantics needed to use the feature correctly.

## Constraints

- Add ordering rules, spread limitations, SSR caveats, or DOM constraints only when needed.

## Examples

### Basic usage

```tsx
function Example() {
	let element!: HTMLDivElement;

	return <div attributeName={element} />;
}
```

## Related

- [`relatedApi`](/reference/path/to/related-api)

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../node_modules/pnpm/bin/pnpm.mjs" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../node_modules/pnpm/bin/pnpm.mjs" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../node_modules/pnpm/bin/pnpm.mjs" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../node_modules/pnpm/bin/pnpm.mjs" "$@"
else
  exec node  "$basedir/../node_modules/pnpm/bin/pnpm.mjs" "$@"
fi
# cmd-shim-target=/Users/ryancarniato/Development/solid-docs/.pnpm-store/v11/links/@/pnpm/11.20.0/0ac5d96351a6410750cfda33be722fc496684c09ac27ac69f12466336af12a33/node_modules/pnpm/bin/pnpm.mjs

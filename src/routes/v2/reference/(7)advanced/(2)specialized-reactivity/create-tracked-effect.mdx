---
title: "createTrackedEffect"
category: "Advanced / Specialized Reactivity & Tracking"
use_cases: "advanced specialized reactivity & tracking api, createtrackedeffect usage"
tags:
  - "create"
  - "tracked"
  - "effect"
  - "advanced"
  - "specialized"
  - "reactivity"
  - "tracking"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Creates a tracked reactive effect where dependency tracking and side effects happen\nin the same scope."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/signals.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:create-tracked-effect:summary */}
Creates a tracked reactive effect where dependency tracking and side effects happen
in the same scope.

WARNING: Because tracking and effects happen in the same scope, this primitive
may run multiple times for a single change or show tearing (reading inconsistent
state). Use only when dynamic subscription patterns require same-scope tracking.

```typescript
createTrackedEffect(compute, options?: { name?: string });
```
{/* /@gen:create-tracked-effect:summary */}

{/* @gen:create-tracked-effect:import */}
## Import

```ts
import { createTrackedEffect } from "solid-js";
```
{/* /@gen:create-tracked-effect:import */}

{/* @gen:create-tracked-effect:signature */}
## Type signature

```ts
function createTrackedEffect(
  compute: () => void | (() => void),
  options?: BaseEffectOptions
): void;
```
{/* /@gen:create-tracked-effect:signature */}

{/* @gen:create-tracked-effect:params */}
## Parameters

### `compute`

A function that contains reactive reads to track and returns an optional cleanup function to run on disposal or before next execution

### `options`

-- name
{/* /@gen:create-tracked-effect:params */}

{/* @gen:create-tracked-effect:examples */}
## Examples

```ts
createTrackedEffect(() => {
  const target = focusedNode();
  if (!target) return;

  const handler = () => log(target.value());
  target.on("change", handler);

  return () => target.off("change", handler);
});
```
{/* /@gen:create-tracked-effect:examples */}

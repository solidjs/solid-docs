---
title: "repeat"
category: "Advanced / JSX Component Primitives"
use_cases: "advanced jsx component primitives api, repeat usage"
tags:
  - "repeat"
  - "advanced"
  - "jsx"
  - "component"
  - "primitives"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Reactively renders a callback `count` times, reusing previously-rendered\nentries when only the count changes. Underlying helper for `<Repeat>`."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/map.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:repeat:summary */}
Reactively renders a callback `count` times, reusing previously-rendered
entries when only the count changes. Underlying helper for `<Repeat>`.

- `options.from` — start index (default `0`); useful for offset/windowed
  rendering.
- `options.fallback` — accessor returning a value to show when count is `0`.
{/* /@gen:repeat:summary */}

{/* @gen:repeat:import */}
## Import

```ts
import { repeat } from "solid-js";
```
{/* /@gen:repeat:import */}

{/* @gen:repeat:signature */}
## Type signature

```ts
function repeat(
  count: Accessor<number>,
  map: (index: number) => any,
  options?: {
    from?: Accessor<number | undefined>;
    fallback?: Accessor<any>;
    name?: string;
  }
): Accessor<any[]>;
```
{/* /@gen:repeat:signature */}

{/* @gen:repeat:examples */}
## Examples

```ts
const view = repeat(count, i => `Item ${i}`, { fallback: () => "empty" });
```
{/* /@gen:repeat:examples */}

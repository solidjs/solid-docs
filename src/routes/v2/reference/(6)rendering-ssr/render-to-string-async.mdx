---
title: "renderToStringAsync"
category: "Rendering & SSR"
use_cases: "rendering & ssr api, rendertostringasync usage"
tags:
  - "render"
  - "to"
  - "string"
  - "async"
  - "rendering"
  - "ssr"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Renders a component tree to an HTML string and awaits all async reads in the\nsubtree before resolving. The returned HTML reflects the fully-settled state\n— no `<Loading>` fallbacks appear in the output."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-web/src/server-mock.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:render-to-string-async:summary */}
Renders a component tree to an HTML string and awaits all async reads in the
subtree before resolving. The returned HTML reflects the fully-settled state
— no `<Loading>` fallbacks appear in the output.

Use this when you want a complete page in one round-trip. For incremental
streaming with progressive boundary resolution, use `renderToStream`.
{/* /@gen:render-to-string-async:summary */}

{/* @gen:render-to-string-async:import */}
## Import

```ts
import { renderToStringAsync } from "@solidjs/web";
```
{/* /@gen:render-to-string-async:import */}

{/* @gen:render-to-string-async:signature */}
## Type signature

```ts
function renderToStringAsync<T>(
  fn: () => T,
  options?: {
    timeoutMs?: number;
    nonce?: string;
    renderId?: string;
    noScripts?: boolean;
    plugins?: any[];
    manifest?: Record<
      string,
      {
        file: string;
        css?: string[];
        isEntry?: boolean;
        isDynamicEntry?: boolean;
        imports?: string[];
      }
    >;
    onError?: (err: any) => void;
  }
): Promise<string>;
```
{/* /@gen:render-to-string-async:signature */}

{/* @gen:render-to-string-async:examples */}
## Examples

```tsx
import { renderToStringAsync } from "@solidjs/web";

const html = await renderToStringAsync(() => <App />);
```
{/* /@gen:render-to-string-async:examples */}

---
title: "Repeat"
category: "Components"
version: "2.0"
description: "Creates a list elements from a count"
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "9015b12998595f9b5ca2447c25f66faf68371165"
source_path: "packages/solid/src/client/flow.ts"
---

{/* Generated by scripts/extract-solid-v2-reference.mjs. Edit the source JSDoc or route map, then regenerate. */}

Creates a list elements from a count

it receives a map function as its child that receives the index and returns a JSX-Element; if the list is empty, an optional fallback is returned:
```typescript
<Repeat count={items.length} fallback={<div>No items</div>}>
  {(index) => <div data-index={index}>{items[index]}</div>}
</Repeat>
```

## Import

```ts
import { Repeat } from "solid-js";
```

## Type signature

```ts
function Repeat<T extends JSX.Element>(props: {
  count: number;
  from?: number | undefined;
  fallback?: JSX.Element;
  children: ((index: number) => T) | T;
});
```

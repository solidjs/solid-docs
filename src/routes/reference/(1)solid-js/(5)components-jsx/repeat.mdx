---
title: "Repeat"
category: "Components (JSX)"
use_cases: "components (jsx) api, repeat usage"
tags:
  - "repeat"
  - "components"
  - "jsx"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Creates a list of elements from a count."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "bad66625e13abb5a86168b8be0db8638097f7b71"
source_path: "packages/solid/src/client/flow.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Creates a list of elements from a count.

Receives a map function as its child that takes the index and returns a JSX
element; if the count is zero, an optional `fallback` is rendered instead.

## Import

```ts
import { Repeat } from "solid-js";
```

## Type signature

```ts
function Repeat<T extends SolidElement>(props: {
	count: number;
	from?: number | undefined;
	fallback?: SolidElement;
	children: ((index: number) => T) | T;
});
```

## Examples

```tsx
<Repeat count={items.length} fallback={<div>No items</div>}>
	{(index) => <div data-index={index}>{items[index]}</div>}
</Repeat>
```

---
title: "flush"
category: "Reactivity"
use_cases: "reactivity api, flush usage"
tags:
  - "flush"
  - "reactivity"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Synchronously processes the pending reactive queue: runs every scheduled\nmemo/effect/computation that has dirty inputs, until the graph is settled."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/core/scheduler.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:flush:summary */}
Synchronously processes the pending reactive queue: runs every scheduled
memo/effect/computation that has dirty inputs, until the graph is settled.

Reactive updates are normally batched onto the microtask queue, so multiple
writes in a row collapse into a single update pass. Call `flush()` when you
need to *observe* the result of those writes synchronously — most commonly
in tests, but also at the boundary of imperative integration code.
{/* /@gen:flush:summary */}

{/* @gen:flush:import */}
## Import

```ts
import { flush } from "solid-js";
```
{/* /@gen:flush:import */}

{/* @gen:flush:signature */}
## Type signature

```ts
function flush(): void;
```
{/* /@gen:flush:signature */}

{/* @gen:flush:examples */}
## Examples

```ts
const [count, setCount] = createSignal(0);
const doubled = createMemo(() => count() * 2);

setCount(5);
flush();
expect(doubled()).toBe(10);
```
{/* /@gen:flush:examples */}

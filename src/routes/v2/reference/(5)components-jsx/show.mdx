---
title: "Show"
category: "Components (JSX)"
version: "2.0"
description: "Conditionally renders its children when `when` is truthy, otherwise renders\nthe optional `fallback`."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid/src/client/flow.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Conditionally renders its children when `when` is truthy, otherwise renders
the optional `fallback`.

The function-child form receives an accessor for the narrowed value — call
it to read. Without `keyed` (default), the child is preserved across
truthy values; with `keyed`, the child remounts whenever the value's
identity changes.

## Import

```ts
import { Show } from "solid-js";
```

## Type signature

```ts
function Show<T, F extends ConditionalRenderCallback<T>>(props: {
	when: T | undefined | null | false;
	keyed?: boolean;
	fallback?: JSX.Element;
	children: ConditionalRenderChildren<T, F>;
}): JSX.Element;
```

## Examples

```tsx
<Show when={user()} fallback={<SignIn />}>
	{(u) => <Greeting name={u().name} />}
</Show>
```

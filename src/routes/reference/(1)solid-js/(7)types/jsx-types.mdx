---
title: "JSX"
category: "Types"
use_cases: "types api, jsx usage"
tags:
  - "jsx"
  - "types"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Selected public types from the `JSX` namespace. Element-specific attributes are available through `JSX.IntrinsicElements`."
source_repo: "solidjs/solid"
source_ref: "next"
source_path: "packages/web/jsx/jsx.d.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Selected public types from the `JSX` namespace. Element-specific attributes are available through `JSX.IntrinsicElements`.

## Import

```ts
import type { JSX } from "@solidjs/web";
```

## Type signature

```ts
namespace JSX {
	type Element = SolidElement | Node | ArrayElement;
	type ClassValue =
		| string
		| number
		| boolean
		| null
		| undefined
		| Record<string, boolean>
		| ClassValue[];
	type RefCallback<T> = (element: T) => void;
	type Ref<T> = T | RefCallback<T> | undefined | Ref<T>[];
	interface CSSProperties extends csstype.PropertiesHyphen {
		[key: `-${string}`]: string | number | undefined;
	}
	interface IntrinsicElements
		extends
			HTMLElementTags,
			HTMLElementDeprecatedTags,
			SVGElementTags,
			MathMLElementTags {}
}
```

## Related types

### `ArrayElement`

```ts
interface ArrayElement extends Array<Element> {}
```

### `Element`

```ts
type Element =
	| RenderedElement
	| ArrayElement
	| (string & {})
	| number
	| boolean
	| null
	| undefined;
```

### `IntrinsicElement`

```ts
type IntrinsicElement = Extract<keyof JSX.IntrinsicElements, string>;
```

### `ResolvedElement`

```ts
type ResolvedElement = Exclude<SolidElement, ArrayElement>;
```

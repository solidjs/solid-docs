---
title: "redirect / isHref"
category: "Request & response"
use_cases: "request & response api, redirect usage"
tags:
  - "redirect"
  - "is"
  - "href"
  - "request"
  - "response"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Creates a redirect `Response` (status 302 by default). Pass `revalidate` to name the cache keys the mutation invalidated so the client refetches them after following the redirect."
source_repo: "solidjs/solid"
source_ref: "next"
source_path: "packages/web/src/response.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Creates a redirect `Response` (status 302 by default). Pass `revalidate` to name the cache keys the mutation invalidated so the client refetches them after following the redirect.

## Import

```ts
import { redirect, isHref } from "@solidjs/web";
```

## `redirect`

### Type signature

```ts
function redirect(
	url: string | Href,
	init?: number | ResponseHelperInit
): Response;
```

### Parameters

#### `url`

- **Type:** `string | Href`

#### `init`

- **Type:** `number | ResponseHelperInit`
- Optional

### Examples

```ts
return redirect("/dashboard", { revalidate: "session" });
```

### Common problems

- [The form submits, but the page reloads instead of staying put](/guides/forms#the-form-submits-but-the-page-reloads-instead-of-staying-put)

## `isHref`

Whether `value` is an `Href`-branded URL-bearing value.

### Type signature

```ts
function isHref(value: unknown): value is Href;
```

### Parameters

#### `value`

- **Type:** `unknown`

## Learn more

- [Mutations and responses](/building-apps/server-functions/mutations-and-responses)
- [Middleware and API routes](/building-apps/middleware-and-api-routes)

## Related types

### `Href`

```ts
interface Href {
  [HREF]: true | string;
  toString(): string;
};
```

#### `[HREF]`

- **Type:** `true | string`

The brand doubles as a channel: when the slot holds a string it is the
value's *logical* path — the routable pathname before an integration's
display rendering (for example, a hash router's `#` prefix). `redirect()` prefers
it over coercion so Location headers carry routable paths; `toString()`
remains the display href for the DOM. `true` brands a value whose
string form is already logical.

#### `toString`

- **Type:** `string`

### `ResponseHelperInit`

`ResponseInit` accepted by the response helpers, plus `revalidate`.

```ts
interface ResponseHelperInit extends ResponseInit {
  revalidate?: string | string[];
};
```

#### `revalidate`

- **Type:** `string | string[]`

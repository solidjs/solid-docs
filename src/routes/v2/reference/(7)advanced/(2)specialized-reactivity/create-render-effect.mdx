---
title: "createRenderEffect"
category: "Advanced / Specialized Reactivity & Tracking"
use_cases: "advanced specialized reactivity & tracking api, createrendereffect usage"
tags:
  - "create"
  - "render"
  - "effect"
  - "advanced"
  - "specialized"
  - "reactivity"
  - "tracking"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Creates a reactive computation that runs during the render phase as DOM elements\nare created and updated but not necessarily connected."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/signals.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:create-render-effect:summary */}
Creates a reactive computation that runs during the render phase as DOM elements
are created and updated but not necessarily connected.

Same compute / effect split as `createEffect`, but scheduled inside the render
queue rather than after it. Reach for this only when authoring renderer
plumbing (custom DOM bindings, JSX-generated `insert()` / `spread()` calls).
App code should use `createEffect`.

```typescript
createRenderEffect<T>(compute, effectFn, options?: EffectOptions);
```
{/* /@gen:create-render-effect:summary */}

{/* @gen:create-render-effect:import */}
## Import

```ts
import { createRenderEffect } from "solid-js";
```
{/* /@gen:create-render-effect:import */}

{/* @gen:create-render-effect:signature */}
## Type signature

```ts
function createRenderEffect<T>(
  compute: ComputeFunction<undefined | NoInfer<T>, T>,
  effectFn: EffectFunction<NoInfer<T>, T>,
  options?: EffectOptions
): void;
```
{/* /@gen:create-render-effect:signature */}

{/* @gen:create-render-effect:params */}
## Parameters

### `compute`

A function that receives its previous value and returns a new value used to react on a computation

### `effectFn`

A function that receives the new value and is used to perform side effects

### `options`

`EffectOptions` -- name, defer, schedule
{/* /@gen:create-render-effect:params */}

{/* @gen:create-render-effect:examples */}
## Examples

```ts
// Custom directive: bind an element's textContent to a reactive source.
function bindText(el: HTMLElement, source: () => string) {
  createRenderEffect(
    () => source(),
    value => { el.textContent = value; }
  );
}
```
{/* /@gen:create-render-effect:examples */}

---
title: "getObserver"
category: "Advanced / Owner & Introspection"
use_cases: "advanced owner & introspection api, getobserver usage"
tags:
  - "get"
  - "observer"
  - "advanced"
  - "owner"
  - "introspection"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Returns the currently-tracking observer (the computation that subscribes to\nreactive reads at this point), or `null` if reads here would be untracked.\nUsed by reactive primitives that need to know whether they're inside a\ntracking scope. App code rarely needs this — see `getOwner()` for the\nlifecycle owner instead."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "a93a216ef35bd053949493506942884fb24e7684"
source_path: "packages/solid-signals/src/core/owner.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit JSDoc/source for generated blocks; text outside [@gen] blocks is preserved. */}

{/* @gen:get-observer:summary */}
Returns the currently-tracking observer (the computation that subscribes to
reactive reads at this point), or `null` if reads here would be untracked.
Used by reactive primitives that need to know whether they're inside a
tracking scope. App code rarely needs this — see `getOwner()` for the
lifecycle owner instead.
{/* /@gen:get-observer:summary */}

{/* @gen:get-observer:import */}
## Import

```ts
import { getObserver } from "solid-js";
```
{/* /@gen:get-observer:import */}

{/* @gen:get-observer:signature */}
## Type signature

```ts
function getObserver(): Owner | null;
```
{/* /@gen:get-observer:signature */}

{/* @gen:get-observer:examples */}
## Examples

```ts
// Library predicate: only register a hot-path subscription when the
// caller is inside a tracking scope (memo / effect compute / JSX).
function trackIfTracked(source: () => unknown) {
  if (getObserver()) source();
}
```
{/* /@gen:get-observer:examples */}

---
title: "createComponent"
category: "Component APIs"
version: "2.0"
description: "Invokes a component, wrapping the call in `untrack` so that reactive reads\ninside the component body don't subscribe the parent computation. Compiled\nJSX uses this internally; manual calls are rarely needed unless authoring a\ncustom JSX factory or renderer."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "9015b12998595f9b5ca2447c25f66faf68371165"
source_path: "packages/solid/src/client/component.ts"
---

{/* Generated by scripts/extract-solid-v2-reference.mjs. Edit the source JSDoc or route map, then regenerate. */}

Invokes a component, wrapping the call in `untrack` so that reactive reads
inside the component body don't subscribe the parent computation. Compiled
JSX uses this internally; manual calls are rarely needed unless authoring a
custom JSX factory or renderer.

## Import

```ts
import { createComponent } from "solid-js";
```

## Type signature

```ts
function createComponent<T extends Record<string, any>>(
  Comp: Component<T>,
  props: T
): JSX.Element;
```

---
title: "Errored"
category: "Components (JSX)"
use_cases: "components (jsx) api, errored usage"
tags:
  - "errored"
  - "components"
  - "jsx"
  - "reference"
  - "api"
  - "v2"
version: "2.0"
description: "Catches uncaught errors inside its subtree and renders fallback content\ninstead. The `fallback` prop can be a JSX element, or a callback that\nreceives the error and a `reset()` function for retry affordances."
source_repo: "solidjs/solid"
source_ref: "next"
source_sha: "bad66625e13abb5a86168b8be0db8638097f7b71"
source_path: "packages/solid/src/client/flow.ts"
---

{/* Generated by scripts/extract-solid-ref.mjs. Edit the source JSDoc or disposition map, then regenerate. */}

Catches uncaught errors inside its subtree and renders fallback content
instead. The `fallback` prop can be a JSX element, or a callback that
receives the error and a `reset()` function for retry affordances.

Errors thrown from the fallback itself can be caught by a parent
`<Errored>`.

## Import

```ts
import { Errored } from "solid-js";
```

## Type signature

```ts
function Errored(props: {
	fallback:
		| SolidElement
		| ((err: ErrorAccessor, reset: () => void) => SolidElement);
	children: SolidElement;
}): SolidElement;
```

## Examples

```tsx
<Errored
	fallback={(err, reset) => (
		<div onClick={reset}>Error: {err().toString()}</div>
	)}
>
	<MyComp />
</Errored>
```
